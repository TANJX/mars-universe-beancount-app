# Copy to .env.local and fill in.
#
# logo.dev publishable key — drives merchant logo image fetching.
# Get one free at https://logo.dev (the `pk_` prefix means it's safe to expose
# in client bundles). Without a token, merchants render as letter marks.
NEXT_PUBLIC_LOGO_DEV_TOKEN=pk_your_token_here
